import {
    StyleSheet
} from 'react-native';

module.exports = StyleSheet.create({
    txnList: {
        backgroundColor: '#FCFCFC',
    },
    txnListEmpty: {
        marginTop: 100,
        fontSize: 15,
        padding: 20,
        textAlign: 'center'
    },
    txnShowAllBtn:{
        backgroundColor: '#FFB400'
    },
    txnShowAllBtnText:{
        fontSize: 16,
        fontWeight: '500',
    },
});
