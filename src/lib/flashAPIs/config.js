import { Platform } from 'react-native';

let APP_VERSION = 1;
let RESOURCE = 'app';
let API_URL = 'https://dev03keys.flashcoin.io/api';

module.exports = {
    API_URL,
    APP_VERSION,
    RESOURCE
}
